---
title: "Core Concepts"
description: "A tour of the five primitives that make HydraDB work."
---

> A short overview of each primitive, with links to deeper [Concepts](/essentials/v2/architecture) pages.

## The five primitives

| Primitive | What it is | Deep dive |
|---|---|---|
| **Memories** | User-scoped context - preferences, conversations, inferred traits | [Usage → Memories](/essentials/v2/memories) |
| **Knowledge** | Shared, tenant-wide context - documents, PDFs, app sources | [Usage → Knowledge](/essentials/v2/knowledge) |
| **Search** | How agents read context from HydraDB | [Usage → Search](/essentials/v2/search) |
| **Tenants** | Isolated workspaces with sub-tenant hierarchy | [Concepts → Multi-Tenant](/essentials/v2/multi-tenant) |
| **Metadata** | Structured filters for deterministic retrieval | [Usage → Metadata](/essentials/v2/metadata) |

**Looking for something else?** [API Reference](/api-reference/v2) · [Quickstart](/get-started/v2/quickstart) · [Architecture](/essentials/v2/architecture)

---

## The mental model

Memories are scoped by sub-tenant. Knowledge is shared inside the tenant. Metadata filters retrieval, and Search is how your agent reads across the structure.

```mermaid
flowchart TB
  Agent["🤖 Your AI Agent"]
  Query["What's our refund policy for Sarah?"]

  Agent --> Query
  Query --> Search["⚙️ Search Engine<br/>filter → retrieve → rank → merge"]

  subgraph Tenant["Tenant: acme_corp"]
    Knowledge["📚 Knowledge<br/>tenant-shared docs & sources<br/><br/>• refund policy = 30 days<br/>• VIP exception exists<br/>• extension workflow"]

    Metadata["🏷️ Metadata Filters<br/>deterministic narrowing<br/><br/>region = US<br/>status = approved<br/>policy_type = refunds"]

    subgraph SubTenant["Sub-Tenant: user_sarah"]
      Memories["🧠 Memories<br/>user-scoped preferences & history<br/><br/>• hates long emails<br/>• prefers bullet points<br/>• prior escalation history"]
    end
  end

  Knowledge --> Search
  Metadata --> Search
  Memories --> Search

  Search --> Response["Agent Response<br/><br/>Refund window is 30 days, but Sarah is VIP,<br/>so the extension workflow applies.<br/><br/>Short, bullet-style answer matching Sarah's preference."]

  classDef agent fill:#8b5cf6,stroke:#a78bfa,color:#f8fafc,stroke-width:2px
  classDef search fill:#1e1b4b,stroke:#8b5cf6,color:#f8fafc,stroke-width:2px
  classDef knowledge fill:#1e293b,stroke:#06b6d4,color:#f8fafc,stroke-width:1.5px
  classDef memory fill:#1e293b,stroke:#34d399,color:#f8fafc,stroke-width:1.5px
  classDef metadata fill:#1e293b,stroke:#f59e0b,color:#f8fafc,stroke-width:1.5px
  classDef response fill:#1e293b,stroke:#a78bfa,color:#f8fafc,stroke-width:1.5px
  classDef scope fill:#0f172a,stroke:#475569,color:#f8fafc,stroke-width:1.5px

  class Agent,Query agent
  class Search search
  class Knowledge knowledge
  class Memories memory
  class Metadata metadata
  class Response response
  class Tenant,SubTenant scope
```

---

## Memories

Memories are a user-scoped context that your agent writes and reads over time - preferences, conversation history, and inferred behavioral traits.

When you ingest a memory with `infer: true`, HydraDB automatically extracts the underlying preference. You can ship raw behavioral logs (interaction events, UI actions, dialogue), and HydraDB derives the structured insight. You don't have to do the extraction.

When you ingest a memory with `infer: false` (the default), HydraDB stores exactly what you send - useful for facts you've already captured.

Memories are retrieved by calling [`POST /search`](/api-reference/v2/endpoint/search) with `source: "memories"`.

Read More: [Usage → Memories](/essentials/v2/memories)

---

## Knowledge

Knowledge is shared, tenant-wide context ingested from documents and app sources - PDFs, DOCX, Markdown, Slack threads, Notion pages, CSVs, emails.

HydraDB parses, chunks, embeds, and links each source into the context graph at write time. Entities and relationships are extracted automatically. At search time, the graph traversal step surfaces structurally connected material that vector search alone would miss.

Knowledge is retrieved by calling [`POST /search`](/api-reference/v2/endpoint/search) with `source: "sources"`. Combine memories and knowledge in one call with `source: "all"`.

Read More: [Usage → Knowledge](/essentials/v2/knowledge)

---

## Why they're separate

Memories and Knowledge are distinct at the storage layer, not just conceptually:

| | Memories | Knowledge |
|---|---|---|
| **Content** | User preferences, conversation history, inferred traits | Documents, files, app-generated content |
| **Scope** | Per-user (scoped by `sub_tenant_id`) | Shared across all users in a tenant |
| **Mutability** | Dynamic - evolves with every interaction | Versioned - replaced or deleted explicitly |
| **Search `source`** | `"memories"` | `"sources"` |
| **Inference** | `infer` flag extracts traits automatically | Not applicable - content is parsed and chunked |

For personalized answers grounded in shared documents, call `POST /search` with `source: "all"` to merge both stores in a single request. See [How to Use API Results](/essentials/v2/api-results).

---

## Search

Search is how agents read from HydraDB. Storing data is easy; knowing *what* to retrieve, *when*, and *why* is the hard part.

HydraDB's search is not a vector search. It's a multi-stage pipeline that combines metadata filtering, semantic and keyword retrieval, [context-graph](/essentials/v2/context-graphs) traversal, and personalized ranking - all in a single API call.

Vector search asks *"what's similar to my query?"* Graph traversal asks *"What's structurally connected to it?"* HydraDB does both, then weights the results by usefulness for the current task.

One endpoint - [`POST /search`](/api-reference/v2/endpoint/search) - covers Knowledge, Memories, or both, tuned by `search_by` (`hybrid` / `text`), `mode` (`thinking` / fast), and `graph_context`.

Read More: [Usage → Search](/essentials/v2/search)

---

## Tenants and Sub-Tenants

A **Tenant** is a completely isolated workspace. No tenant can read another tenant's data. In most cases, each organization has one tenant.

A **sub-tenant** is a logical partition within a tenant - a user, team, project, or department.

- **B2B** - each customer is a tenant; their departments are sub-tenants
- **B2C** - one tenant for the organization; each end user is a sub-tenant

Read More: [Concepts → Multi-Tenant Support](/essentials/v2/multi-tenant)

---

## Metadata

Metadata makes search deterministic. Semantic search is powerful, but production systems need hard filters: "only Engineering docs," "only approved policies," "only refund policies."

Two tiers:

- **`metadata`** - filterable fields defined in the tenant schema. Plan these fields before ingestion and pass them through `metadata_filters` at search time.
- **`additional_metadata`** - free-form fields attached per document at ingestion. Flexible. Not matchable at search time.

At ingestion:

```json
{
  "metadata": { "compliance_framework": "SOC2", "region": "us" },
  "additional_metadata": {
    "owner": "sarah.chen@acme.com",
    "status": "approved"
 }
}
```

At query time:

```json
{
  "query": "latest pricing strategy",
  "metadata_filters": { "compliance_framework": "SOC2" }
}
```

Filters apply *before* semantic ranking - turning "semantically similar" into "semantically similar *and* properly scoped."

Read More: [Usage → Metadata](/essentials/v2/metadata)

---

## What's next

- [Architecture Overview](/essentials/v2/architecture) - how the graph, vector store, and ranking layers fit together
- [Quickstart](/get-started/v2/quickstart) - build your first integration in five minutes
- [Concepts](/essentials/v2/architecture) - deeper coverage of every concept on this page
